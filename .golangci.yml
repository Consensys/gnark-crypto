version: "2"
run:
  go: "1.25"
  issues-exit-code: 1
linters:
  default: none
  enable:
    - staticcheck
    - gosec
    - govet
    - ineffassign
    - misspell

  settings:
    gosec:
      excludes:
        - G115
        - G602
  exclusions:
    generated: disable
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    paths:
      - third_party$
      - builtin$
      - examples$
formatters:
  enable:
    - gofmt
  exclusions:
    generated: disable
    paths:
      - third_party$
      - builtin$
      - examples$
