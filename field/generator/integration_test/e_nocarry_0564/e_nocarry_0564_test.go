// Copyright 2020 ConsenSys Software Inc.
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

// Code generated by consensys/gnark-crypto DO NOT EDIT

package integration

import (
	"crypto/rand"
	"math/big"
	"math/bits"
	mrand "math/rand"
	"testing"

	"github.com/leanovate/gopter"
	"github.com/leanovate/gopter/prop"
)

// -------------------------------------------------------------------------------------------------
// benchmarks
// most benchmarks are rudimentary and should sample a large number of random inputs
// or be run multiple times to ensure it didn't measure the fastest path of the function

var benchRese_nocarry_0564 e_nocarry_0564

func BenchmarkE_nocarry_0564SetBytes(b *testing.B) {
	var x e_nocarry_0564
	x.SetRandom()
	bb := x.Bytes()
	b.ResetTimer()

	for i := 0; i < b.N; i++ {
		benchRese_nocarry_0564.SetBytes(bb[:])
	}

}

func BenchmarkE_nocarry_0564MulByConstants(b *testing.B) {
	b.Run("mulBy3", func(b *testing.B) {
		benchRese_nocarry_0564.SetRandom()
		b.ResetTimer()
		for i := 0; i < b.N; i++ {
			MulBy3(&benchRese_nocarry_0564)
		}
	})
	b.Run("mulBy5", func(b *testing.B) {
		benchRese_nocarry_0564.SetRandom()
		b.ResetTimer()
		for i := 0; i < b.N; i++ {
			MulBy5(&benchRese_nocarry_0564)
		}
	})
	b.Run("mulBy13", func(b *testing.B) {
		benchRese_nocarry_0564.SetRandom()
		b.ResetTimer()
		for i := 0; i < b.N; i++ {
			MulBy13(&benchRese_nocarry_0564)
		}
	})
}

func BenchmarkE_nocarry_0564Inverse(b *testing.B) {
	var x e_nocarry_0564
	x.SetRandom()
	benchRese_nocarry_0564.SetRandom()
	b.ResetTimer()

	for i := 0; i < b.N; i++ {
		benchRese_nocarry_0564.Inverse(&x)
	}

}

func BenchmarkE_nocarry_0564Butterfly(b *testing.B) {
	var x e_nocarry_0564
	x.SetRandom()
	benchRese_nocarry_0564.SetRandom()
	b.ResetTimer()
	for i := 0; i < b.N; i++ {
		Butterfly(&x, &benchRese_nocarry_0564)
	}
}

func BenchmarkE_nocarry_0564Exp(b *testing.B) {
	var x e_nocarry_0564
	x.SetRandom()
	benchRese_nocarry_0564.SetRandom()
	b1, _ := rand.Int(rand.Reader, Modulus())
	b.ResetTimer()
	for i := 0; i < b.N; i++ {
		benchRese_nocarry_0564.Exp(x, b1)
	}
}

func BenchmarkE_nocarry_0564Double(b *testing.B) {
	benchRese_nocarry_0564.SetRandom()
	b.ResetTimer()
	for i := 0; i < b.N; i++ {
		benchRese_nocarry_0564.Double(&benchRese_nocarry_0564)
	}
}

func BenchmarkE_nocarry_0564Add(b *testing.B) {
	var x e_nocarry_0564
	x.SetRandom()
	benchRese_nocarry_0564.SetRandom()
	b.ResetTimer()
	for i := 0; i < b.N; i++ {
		benchRese_nocarry_0564.Add(&x, &benchRese_nocarry_0564)
	}
}

func BenchmarkE_nocarry_0564Sub(b *testing.B) {
	var x e_nocarry_0564
	x.SetRandom()
	benchRese_nocarry_0564.SetRandom()
	b.ResetTimer()
	for i := 0; i < b.N; i++ {
		benchRese_nocarry_0564.Sub(&x, &benchRese_nocarry_0564)
	}
}

func BenchmarkE_nocarry_0564Neg(b *testing.B) {
	benchRese_nocarry_0564.SetRandom()
	b.ResetTimer()
	for i := 0; i < b.N; i++ {
		benchRese_nocarry_0564.Neg(&benchRese_nocarry_0564)
	}
}

func BenchmarkE_nocarry_0564Div(b *testing.B) {
	var x e_nocarry_0564
	x.SetRandom()
	benchRese_nocarry_0564.SetRandom()
	b.ResetTimer()
	for i := 0; i < b.N; i++ {
		benchRese_nocarry_0564.Div(&x, &benchRese_nocarry_0564)
	}
}

func BenchmarkE_nocarry_0564FromMont(b *testing.B) {
	benchRese_nocarry_0564.SetRandom()
	b.ResetTimer()
	for i := 0; i < b.N; i++ {
		benchRese_nocarry_0564.FromMont()
	}
}

func BenchmarkE_nocarry_0564ToMont(b *testing.B) {
	benchRese_nocarry_0564.SetRandom()
	b.ResetTimer()
	for i := 0; i < b.N; i++ {
		benchRese_nocarry_0564.ToMont()
	}
}
func BenchmarkE_nocarry_0564Square(b *testing.B) {
	benchRese_nocarry_0564.SetRandom()
	b.ResetTimer()
	for i := 0; i < b.N; i++ {
		benchRese_nocarry_0564.Square(&benchRese_nocarry_0564)
	}
}

func BenchmarkE_nocarry_0564Sqrt(b *testing.B) {
	var a e_nocarry_0564
	a.SetRandom()
	b.ResetTimer()
	for i := 0; i < b.N; i++ {
		benchRese_nocarry_0564.Sqrt(&a)
	}
}

func BenchmarkE_nocarry_0564Mul(b *testing.B) {
	x := e_nocarry_0564{
		18255402744885017663,
		4982340957740230593,
		14165937388544439405,
		13942813797843395988,
		2192318434131589801,
		15738633356989088343,
		1112928584183765533,
		2638143367978453937,
		300843310389994,
	}
	benchRese_nocarry_0564.SetOne()
	b.ResetTimer()
	for i := 0; i < b.N; i++ {
		benchRese_nocarry_0564.Mul(&benchRese_nocarry_0564, &x)
	}
}

func BenchmarkE_nocarry_0564Cmp(b *testing.B) {
	x := e_nocarry_0564{
		18255402744885017663,
		4982340957740230593,
		14165937388544439405,
		13942813797843395988,
		2192318434131589801,
		15738633356989088343,
		1112928584183765533,
		2638143367978453937,
		300843310389994,
	}
	benchRese_nocarry_0564 = x
	benchRese_nocarry_0564[0] = 0
	b.ResetTimer()
	for i := 0; i < b.N; i++ {
		benchRese_nocarry_0564.Cmp(&x)
	}
}

func TestE_nocarry_0564Cmp(t *testing.T) {
	var x, y e_nocarry_0564

	if x.Cmp(&y) != 0 {
		t.Fatal("x == y")
	}

	one := One()
	y.Sub(&y, &one)

	if x.Cmp(&y) != -1 {
		t.Fatal("x < y")
	}
	if y.Cmp(&x) != 1 {
		t.Fatal("x < y")
	}

	x = y
	if x.Cmp(&y) != 0 {
		t.Fatal("x == y")
	}

	x.Sub(&x, &one)
	if x.Cmp(&y) != -1 {
		t.Fatal("x < y")
	}
	if y.Cmp(&x) != 1 {
		t.Fatal("x < y")
	}
}

func TestE_nocarry_0564IsRandom(t *testing.T) {
	for i := 0; i < 50; i++ {
		var x, y e_nocarry_0564
		x.SetRandom()
		y.SetRandom()
		if x.Equal(&y) {
			t.Fatal("2 random numbers are unlikely to be equal")
		}
	}
}

// -------------------------------------------------------------------------------------------------
// Gopter tests
// most of them are generated with a template

const (
	nbFuzzShort = 20
	nbFuzz      = 100
)

// special values to be used in tests
var staticTestValues []e_nocarry_0564

func init() {
	staticTestValues = append(staticTestValues, e_nocarry_0564{}) // zero
	staticTestValues = append(staticTestValues, One())            // one
	staticTestValues = append(staticTestValues, rSquare)          // r^2
	var e, one e_nocarry_0564
	one.SetOne()
	e.Sub(&qe_nocarry_0564, &one)
	staticTestValues = append(staticTestValues, e) // q - 1
	e.Double(&one)
	staticTestValues = append(staticTestValues, e) // 2

	{
		a := qe_nocarry_0564
		a[8]--
		staticTestValues = append(staticTestValues, a)
	}
	{
		a := qe_nocarry_0564
		a[0]--
		staticTestValues = append(staticTestValues, a)
	}

	for i := 0; i <= 3; i++ {
		staticTestValues = append(staticTestValues, e_nocarry_0564{uint64(i)})
		staticTestValues = append(staticTestValues, e_nocarry_0564{0, uint64(i)})
	}

	{
		a := qe_nocarry_0564
		a[8]--
		a[0]++
		staticTestValues = append(staticTestValues, a)
	}

}

func TestE_nocarry_0564NegZero(t *testing.T) {
	var a, b e_nocarry_0564
	b.SetZero()
	for a.IsZero() {
		a.SetRandom()
	}
	a.Neg(&b)
	if !a.IsZero() {
		t.Fatal("neg(0) != 0")
	}
}

func TestE_nocarry_0564Reduce(t *testing.T) {
	testValues := make([]e_nocarry_0564, len(staticTestValues))
	copy(testValues, staticTestValues)

	for _, s := range testValues {
		expected := s
		reduce(&s)
		_reduceGeneric(&expected)
		if !s.Equal(&expected) {
			t.Fatal("reduce failed: asm and generic impl don't match")
		}
	}

	parameters := gopter.DefaultTestParameters()
	if testing.Short() {
		parameters.MinSuccessfulTests = nbFuzzShort
	} else {
		parameters.MinSuccessfulTests = nbFuzz
	}

	properties := gopter.NewProperties(parameters)

	genA := genFull()

	properties.Property("reduce should output a result smaller than modulus", prop.ForAll(
		func(a e_nocarry_0564) bool {
			b := a
			reduce(&a)
			_reduceGeneric(&b)
			return !a.biggerOrEqualModulus() && a.Equal(&b)
		},
		genA,
	))

	properties.TestingRun(t, gopter.ConsoleReporter(false))
	// if we have ADX instruction enabled, test both path in assembly
	if supportAdx {
		t.Log("disabling ADX")
		supportAdx = false
		properties.TestingRun(t, gopter.ConsoleReporter(false))
		supportAdx = true
	}

}

func TestE_nocarry_0564Bytes(t *testing.T) {
	parameters := gopter.DefaultTestParameters()
	if testing.Short() {
		parameters.MinSuccessfulTests = nbFuzzShort
	} else {
		parameters.MinSuccessfulTests = nbFuzz
	}

	properties := gopter.NewProperties(parameters)

	genA := gen()

	properties.Property("SetBytes(Bytes()) should stayt constant", prop.ForAll(
		func(a testPaire_nocarry_0564) bool {
			var b e_nocarry_0564
			bytes := a.element.Bytes()
			b.SetBytes(bytes[:])
			return a.element.Equal(&b)
		},
		genA,
	))

	properties.TestingRun(t, gopter.ConsoleReporter(false))
}

func TestE_nocarry_0564InverseExp(t *testing.T) {
	// inverse must be equal to exp^-2
	exp := Modulus()
	exp.Sub(exp, new(big.Int).SetUint64(2))

	parameters := gopter.DefaultTestParameters()
	if testing.Short() {
		parameters.MinSuccessfulTests = nbFuzzShort
	} else {
		parameters.MinSuccessfulTests = nbFuzz
	}

	properties := gopter.NewProperties(parameters)

	genA := gen()

	properties.Property("inv == exp^-2", prop.ForAll(
		func(a testPaire_nocarry_0564) bool {
			var b e_nocarry_0564
			b.Set(&a.element)
			a.element.Inverse(&a.element)
			b.Exp(b, exp)

			return a.element.Equal(&b)
		},
		genA,
	))

	properties.TestingRun(t, gopter.ConsoleReporter(false))
	// if we have ADX instruction enabled, test both path in assembly
	if supportAdx {
		t.Log("disabling ADX")
		supportAdx = false
		properties.TestingRun(t, gopter.ConsoleReporter(false))
		supportAdx = true
	}
}

func TestE_nocarry_0564MulByConstants(t *testing.T) {

	parameters := gopter.DefaultTestParameters()
	if testing.Short() {
		parameters.MinSuccessfulTests = nbFuzzShort
	} else {
		parameters.MinSuccessfulTests = nbFuzz
	}

	properties := gopter.NewProperties(parameters)

	genA := gen()

	implemented := []uint8{0, 1, 2, 3, 5, 13}
	properties.Property("mulByConstant", prop.ForAll(
		func(a testPaire_nocarry_0564) bool {
			for _, c := range implemented {
				var constant e_nocarry_0564
				constant.SetUint64(uint64(c))

				b := a.element
				b.Mul(&b, &constant)

				aa := a.element
				mulByConstant(&aa, c)

				if !aa.Equal(&b) {
					return false
				}
			}

			return true
		},
		genA,
	))

	properties.Property("MulBy3(x) == Mul(x, 3)", prop.ForAll(
		func(a testPaire_nocarry_0564) bool {
			var constant e_nocarry_0564
			constant.SetUint64(3)

			b := a.element
			b.Mul(&b, &constant)

			MulBy3(&a.element)

			return a.element.Equal(&b)
		},
		genA,
	))

	properties.Property("MulBy5(x) == Mul(x, 5)", prop.ForAll(
		func(a testPaire_nocarry_0564) bool {
			var constant e_nocarry_0564
			constant.SetUint64(5)

			b := a.element
			b.Mul(&b, &constant)

			MulBy5(&a.element)

			return a.element.Equal(&b)
		},
		genA,
	))

	properties.Property("MulBy13(x) == Mul(x, 13)", prop.ForAll(
		func(a testPaire_nocarry_0564) bool {
			var constant e_nocarry_0564
			constant.SetUint64(13)

			b := a.element
			b.Mul(&b, &constant)

			MulBy13(&a.element)

			return a.element.Equal(&b)
		},
		genA,
	))

	properties.TestingRun(t, gopter.ConsoleReporter(false))
	// if we have ADX instruction enabled, test both path in assembly
	if supportAdx {
		t.Log("disabling ADX")
		supportAdx = false
		properties.TestingRun(t, gopter.ConsoleReporter(false))
		supportAdx = true
	}

}

func TestE_nocarry_0564Legendre(t *testing.T) {
	parameters := gopter.DefaultTestParameters()
	if testing.Short() {
		parameters.MinSuccessfulTests = nbFuzzShort
	} else {
		parameters.MinSuccessfulTests = nbFuzz
	}

	properties := gopter.NewProperties(parameters)

	genA := gen()

	properties.Property("legendre should output same result than big.Int.Jacobi", prop.ForAll(
		func(a testPaire_nocarry_0564) bool {
			return a.element.Legendre() == big.Jacobi(&a.bigint, Modulus())
		},
		genA,
	))

	properties.TestingRun(t, gopter.ConsoleReporter(false))
	// if we have ADX instruction enabled, test both path in assembly
	if supportAdx {
		t.Log("disabling ADX")
		supportAdx = false
		properties.TestingRun(t, gopter.ConsoleReporter(false))
		supportAdx = true
	}

}

func TestE_nocarry_0564Butterflies(t *testing.T) {

	parameters := gopter.DefaultTestParameters()
	if testing.Short() {
		parameters.MinSuccessfulTests = nbFuzzShort
	} else {
		parameters.MinSuccessfulTests = nbFuzz
	}

	properties := gopter.NewProperties(parameters)

	genA := gen()

	properties.Property("butterfly0 == a -b; a +b", prop.ForAll(
		func(a, b testPaire_nocarry_0564) bool {
			a0, b0 := a.element, b.element

			_butterflyGeneric(&a.element, &b.element)
			Butterfly(&a0, &b0)

			return a.element.Equal(&a0) && b.element.Equal(&b0)
		},
		genA,
		genA,
	))

	properties.TestingRun(t, gopter.ConsoleReporter(false))
	// if we have ADX instruction enabled, test both path in assembly
	if supportAdx {
		t.Log("disabling ADX")
		supportAdx = false
		properties.TestingRun(t, gopter.ConsoleReporter(false))
		supportAdx = true
	}

}

func TestE_nocarry_0564LexicographicallyLargest(t *testing.T) {
	parameters := gopter.DefaultTestParameters()
	if testing.Short() {
		parameters.MinSuccessfulTests = nbFuzzShort
	} else {
		parameters.MinSuccessfulTests = nbFuzz
	}

	properties := gopter.NewProperties(parameters)

	genA := gen()

	properties.Property("element.Cmp should match LexicographicallyLargest output", prop.ForAll(
		func(a testPaire_nocarry_0564) bool {
			var negA e_nocarry_0564
			negA.Neg(&a.element)

			cmpResult := a.element.Cmp(&negA)
			lResult := a.element.LexicographicallyLargest()

			if lResult && cmpResult == 1 {
				return true
			}
			if !lResult && cmpResult != 1 {
				return true
			}
			return false
		},
		genA,
	))

	properties.TestingRun(t, gopter.ConsoleReporter(false))
	// if we have ADX instruction enabled, test both path in assembly
	if supportAdx {
		t.Log("disabling ADX")
		supportAdx = false
		properties.TestingRun(t, gopter.ConsoleReporter(false))
		supportAdx = true
	}

}

func TestE_nocarry_0564Add(t *testing.T) {
	parameters := gopter.DefaultTestParameters()
	if testing.Short() {
		parameters.MinSuccessfulTests = nbFuzzShort
	} else {
		parameters.MinSuccessfulTests = nbFuzz
	}

	properties := gopter.NewProperties(parameters)

	genA := gen()
	genB := gen()

	properties.Property("Add: having the receiver as operand should output the same result", prop.ForAll(
		func(a, b testPaire_nocarry_0564) bool {
			var c, d e_nocarry_0564
			d.Set(&a.element)

			c.Add(&a.element, &b.element)
			a.element.Add(&a.element, &b.element)
			b.element.Add(&d, &b.element)

			return a.element.Equal(&b.element) && a.element.Equal(&c) && b.element.Equal(&c)
		},
		genA,
		genB,
	))

	properties.Property("Add: operation result must match big.Int result", prop.ForAll(
		func(a, b testPaire_nocarry_0564) bool {
			{
				var c e_nocarry_0564

				c.Add(&a.element, &b.element)

				var d, e big.Int
				d.Add(&a.bigint, &b.bigint).Mod(&d, Modulus())

				if c.FromMont().ToBigInt(&e).Cmp(&d) != 0 {
					return false
				}
			}

			// fixed elements
			// a is random
			// r takes special values
			testValues := make([]e_nocarry_0564, len(staticTestValues))
			copy(testValues, staticTestValues)

			for _, r := range testValues {
				var d, e, rb big.Int
				r.ToBigIntRegular(&rb)

				var c e_nocarry_0564
				c.Add(&a.element, &r)
				d.Add(&a.bigint, &rb).Mod(&d, Modulus())

				// checking generic impl against asm path
				var cGeneric e_nocarry_0564
				_addGeneric(&cGeneric, &a.element, &r)
				if !cGeneric.Equal(&c) {
					// need to give context to failing error.
					return false
				}

				if c.FromMont().ToBigInt(&e).Cmp(&d) != 0 {
					return false
				}
			}
			return true
		},
		genA,
		genB,
	))

	properties.Property("Add: operation result must be smaller than modulus", prop.ForAll(
		func(a, b testPaire_nocarry_0564) bool {
			var c e_nocarry_0564

			c.Add(&a.element, &b.element)

			return !c.biggerOrEqualModulus()
		},
		genA,
		genB,
	))

	properties.Property("Add: assembly implementation must be consistent with generic one", prop.ForAll(
		func(a, b testPaire_nocarry_0564) bool {
			var c, d e_nocarry_0564
			c.Add(&a.element, &b.element)
			_addGeneric(&d, &a.element, &b.element)
			return c.Equal(&d)
		},
		genA,
		genB,
	))

	specialValueTest := func() {
		// test special values against special values
		testValues := make([]e_nocarry_0564, len(staticTestValues))
		copy(testValues, staticTestValues)

		for _, a := range testValues {
			var aBig big.Int
			a.ToBigIntRegular(&aBig)
			for _, b := range testValues {

				var bBig, d, e big.Int
				b.ToBigIntRegular(&bBig)

				var c e_nocarry_0564
				c.Add(&a, &b)
				d.Add(&aBig, &bBig).Mod(&d, Modulus())

				// checking asm against generic impl
				var cGeneric e_nocarry_0564
				_addGeneric(&cGeneric, &a, &b)
				if !cGeneric.Equal(&c) {
					t.Fatal("Add failed special test values: asm and generic impl don't match")
				}

				if c.FromMont().ToBigInt(&e).Cmp(&d) != 0 {
					t.Fatal("Add failed special test values")
				}
			}
		}
	}

	properties.TestingRun(t, gopter.ConsoleReporter(false))
	specialValueTest()
	// if we have ADX instruction enabled, test both path in assembly
	if supportAdx {
		t.Log("disabling ADX")
		supportAdx = false
		properties.TestingRun(t, gopter.ConsoleReporter(false))
		specialValueTest()
		supportAdx = true
	}
}

func TestE_nocarry_0564Sub(t *testing.T) {
	parameters := gopter.DefaultTestParameters()
	if testing.Short() {
		parameters.MinSuccessfulTests = nbFuzzShort
	} else {
		parameters.MinSuccessfulTests = nbFuzz
	}

	properties := gopter.NewProperties(parameters)

	genA := gen()
	genB := gen()

	properties.Property("Sub: having the receiver as operand should output the same result", prop.ForAll(
		func(a, b testPaire_nocarry_0564) bool {
			var c, d e_nocarry_0564
			d.Set(&a.element)

			c.Sub(&a.element, &b.element)
			a.element.Sub(&a.element, &b.element)
			b.element.Sub(&d, &b.element)

			return a.element.Equal(&b.element) && a.element.Equal(&c) && b.element.Equal(&c)
		},
		genA,
		genB,
	))

	properties.Property("Sub: operation result must match big.Int result", prop.ForAll(
		func(a, b testPaire_nocarry_0564) bool {
			{
				var c e_nocarry_0564

				c.Sub(&a.element, &b.element)

				var d, e big.Int
				d.Sub(&a.bigint, &b.bigint).Mod(&d, Modulus())

				if c.FromMont().ToBigInt(&e).Cmp(&d) != 0 {
					return false
				}
			}

			// fixed elements
			// a is random
			// r takes special values
			testValues := make([]e_nocarry_0564, len(staticTestValues))
			copy(testValues, staticTestValues)

			for _, r := range testValues {
				var d, e, rb big.Int
				r.ToBigIntRegular(&rb)

				var c e_nocarry_0564
				c.Sub(&a.element, &r)
				d.Sub(&a.bigint, &rb).Mod(&d, Modulus())

				// checking generic impl against asm path
				var cGeneric e_nocarry_0564
				_subGeneric(&cGeneric, &a.element, &r)
				if !cGeneric.Equal(&c) {
					// need to give context to failing error.
					return false
				}

				if c.FromMont().ToBigInt(&e).Cmp(&d) != 0 {
					return false
				}
			}
			return true
		},
		genA,
		genB,
	))

	properties.Property("Sub: operation result must be smaller than modulus", prop.ForAll(
		func(a, b testPaire_nocarry_0564) bool {
			var c e_nocarry_0564

			c.Sub(&a.element, &b.element)

			return !c.biggerOrEqualModulus()
		},
		genA,
		genB,
	))

	properties.Property("Sub: assembly implementation must be consistent with generic one", prop.ForAll(
		func(a, b testPaire_nocarry_0564) bool {
			var c, d e_nocarry_0564
			c.Sub(&a.element, &b.element)
			_subGeneric(&d, &a.element, &b.element)
			return c.Equal(&d)
		},
		genA,
		genB,
	))

	specialValueTest := func() {
		// test special values against special values
		testValues := make([]e_nocarry_0564, len(staticTestValues))
		copy(testValues, staticTestValues)

		for _, a := range testValues {
			var aBig big.Int
			a.ToBigIntRegular(&aBig)
			for _, b := range testValues {

				var bBig, d, e big.Int
				b.ToBigIntRegular(&bBig)

				var c e_nocarry_0564
				c.Sub(&a, &b)
				d.Sub(&aBig, &bBig).Mod(&d, Modulus())

				// checking asm against generic impl
				var cGeneric e_nocarry_0564
				_subGeneric(&cGeneric, &a, &b)
				if !cGeneric.Equal(&c) {
					t.Fatal("Sub failed special test values: asm and generic impl don't match")
				}

				if c.FromMont().ToBigInt(&e).Cmp(&d) != 0 {
					t.Fatal("Sub failed special test values")
				}
			}
		}
	}

	properties.TestingRun(t, gopter.ConsoleReporter(false))
	specialValueTest()
	// if we have ADX instruction enabled, test both path in assembly
	if supportAdx {
		t.Log("disabling ADX")
		supportAdx = false
		properties.TestingRun(t, gopter.ConsoleReporter(false))
		specialValueTest()
		supportAdx = true
	}
}

func TestE_nocarry_0564Mul(t *testing.T) {
	parameters := gopter.DefaultTestParameters()
	if testing.Short() {
		parameters.MinSuccessfulTests = nbFuzzShort
	} else {
		parameters.MinSuccessfulTests = nbFuzz
	}

	properties := gopter.NewProperties(parameters)

	genA := gen()
	genB := gen()

	properties.Property("Mul: having the receiver as operand should output the same result", prop.ForAll(
		func(a, b testPaire_nocarry_0564) bool {
			var c, d e_nocarry_0564
			d.Set(&a.element)

			c.Mul(&a.element, &b.element)
			a.element.Mul(&a.element, &b.element)
			b.element.Mul(&d, &b.element)

			return a.element.Equal(&b.element) && a.element.Equal(&c) && b.element.Equal(&c)
		},
		genA,
		genB,
	))

	properties.Property("Mul: operation result must match big.Int result", prop.ForAll(
		func(a, b testPaire_nocarry_0564) bool {
			{
				var c e_nocarry_0564

				c.Mul(&a.element, &b.element)

				var d, e big.Int
				d.Mul(&a.bigint, &b.bigint).Mod(&d, Modulus())

				if c.FromMont().ToBigInt(&e).Cmp(&d) != 0 {
					return false
				}
			}

			// fixed elements
			// a is random
			// r takes special values
			testValues := make([]e_nocarry_0564, len(staticTestValues))
			copy(testValues, staticTestValues)

			for _, r := range testValues {
				var d, e, rb big.Int
				r.ToBigIntRegular(&rb)

				var c e_nocarry_0564
				c.Mul(&a.element, &r)
				d.Mul(&a.bigint, &rb).Mod(&d, Modulus())

				// checking generic impl against asm path
				var cGeneric e_nocarry_0564
				_mulGeneric(&cGeneric, &a.element, &r)
				if !cGeneric.Equal(&c) {
					// need to give context to failing error.
					return false
				}

				if c.FromMont().ToBigInt(&e).Cmp(&d) != 0 {
					return false
				}
			}
			return true
		},
		genA,
		genB,
	))

	properties.Property("Mul: operation result must be smaller than modulus", prop.ForAll(
		func(a, b testPaire_nocarry_0564) bool {
			var c e_nocarry_0564

			c.Mul(&a.element, &b.element)

			return !c.biggerOrEqualModulus()
		},
		genA,
		genB,
	))

	properties.Property("Mul: assembly implementation must be consistent with generic one", prop.ForAll(
		func(a, b testPaire_nocarry_0564) bool {
			var c, d e_nocarry_0564
			c.Mul(&a.element, &b.element)
			_mulGeneric(&d, &a.element, &b.element)
			return c.Equal(&d)
		},
		genA,
		genB,
	))

	specialValueTest := func() {
		// test special values against special values
		testValues := make([]e_nocarry_0564, len(staticTestValues))
		copy(testValues, staticTestValues)

		for _, a := range testValues {
			var aBig big.Int
			a.ToBigIntRegular(&aBig)
			for _, b := range testValues {

				var bBig, d, e big.Int
				b.ToBigIntRegular(&bBig)

				var c e_nocarry_0564
				c.Mul(&a, &b)
				d.Mul(&aBig, &bBig).Mod(&d, Modulus())

				// checking asm against generic impl
				var cGeneric e_nocarry_0564
				_mulGeneric(&cGeneric, &a, &b)
				if !cGeneric.Equal(&c) {
					t.Fatal("Mul failed special test values: asm and generic impl don't match")
				}

				if c.FromMont().ToBigInt(&e).Cmp(&d) != 0 {
					t.Fatal("Mul failed special test values")
				}
			}
		}
	}

	properties.TestingRun(t, gopter.ConsoleReporter(false))
	specialValueTest()
	// if we have ADX instruction enabled, test both path in assembly
	if supportAdx {
		t.Log("disabling ADX")
		supportAdx = false
		properties.TestingRun(t, gopter.ConsoleReporter(false))
		specialValueTest()
		supportAdx = true
	}
}

func TestE_nocarry_0564Div(t *testing.T) {
	parameters := gopter.DefaultTestParameters()
	if testing.Short() {
		parameters.MinSuccessfulTests = nbFuzzShort
	} else {
		parameters.MinSuccessfulTests = nbFuzz
	}

	properties := gopter.NewProperties(parameters)

	genA := gen()
	genB := gen()

	properties.Property("Div: having the receiver as operand should output the same result", prop.ForAll(
		func(a, b testPaire_nocarry_0564) bool {
			var c, d e_nocarry_0564
			d.Set(&a.element)

			c.Div(&a.element, &b.element)
			a.element.Div(&a.element, &b.element)
			b.element.Div(&d, &b.element)

			return a.element.Equal(&b.element) && a.element.Equal(&c) && b.element.Equal(&c)
		},
		genA,
		genB,
	))

	properties.Property("Div: operation result must match big.Int result", prop.ForAll(
		func(a, b testPaire_nocarry_0564) bool {
			{
				var c e_nocarry_0564

				c.Div(&a.element, &b.element)

				var d, e big.Int
				d.ModInverse(&b.bigint, Modulus())
				d.Mul(&d, &a.bigint).Mod(&d, Modulus())

				if c.FromMont().ToBigInt(&e).Cmp(&d) != 0 {
					return false
				}
			}

			// fixed elements
			// a is random
			// r takes special values
			testValues := make([]e_nocarry_0564, len(staticTestValues))
			copy(testValues, staticTestValues)

			for _, r := range testValues {
				var d, e, rb big.Int
				r.ToBigIntRegular(&rb)

				var c e_nocarry_0564
				c.Div(&a.element, &r)
				d.ModInverse(&rb, Modulus())
				d.Mul(&d, &a.bigint).Mod(&d, Modulus())

				if c.FromMont().ToBigInt(&e).Cmp(&d) != 0 {
					return false
				}
			}
			return true
		},
		genA,
		genB,
	))

	properties.Property("Div: operation result must be smaller than modulus", prop.ForAll(
		func(a, b testPaire_nocarry_0564) bool {
			var c e_nocarry_0564

			c.Div(&a.element, &b.element)

			return !c.biggerOrEqualModulus()
		},
		genA,
		genB,
	))

	specialValueTest := func() {
		// test special values against special values
		testValues := make([]e_nocarry_0564, len(staticTestValues))
		copy(testValues, staticTestValues)

		for _, a := range testValues {
			var aBig big.Int
			a.ToBigIntRegular(&aBig)
			for _, b := range testValues {

				var bBig, d, e big.Int
				b.ToBigIntRegular(&bBig)

				var c e_nocarry_0564
				c.Div(&a, &b)
				d.ModInverse(&bBig, Modulus())
				d.Mul(&d, &aBig).Mod(&d, Modulus())

				if c.FromMont().ToBigInt(&e).Cmp(&d) != 0 {
					t.Fatal("Div failed special test values")
				}
			}
		}
	}

	properties.TestingRun(t, gopter.ConsoleReporter(false))
	specialValueTest()
	// if we have ADX instruction enabled, test both path in assembly
	if supportAdx {
		t.Log("disabling ADX")
		supportAdx = false
		properties.TestingRun(t, gopter.ConsoleReporter(false))
		specialValueTest()
		supportAdx = true
	}
}

func TestE_nocarry_0564Exp(t *testing.T) {
	parameters := gopter.DefaultTestParameters()
	if testing.Short() {
		parameters.MinSuccessfulTests = nbFuzzShort
	} else {
		parameters.MinSuccessfulTests = nbFuzz
	}

	properties := gopter.NewProperties(parameters)

	genA := gen()
	genB := gen()

	properties.Property("Exp: having the receiver as operand should output the same result", prop.ForAll(
		func(a, b testPaire_nocarry_0564) bool {
			var c, d e_nocarry_0564
			d.Set(&a.element)

			c.Exp(a.element, &b.bigint)
			a.element.Exp(a.element, &b.bigint)
			b.element.Exp(d, &b.bigint)

			return a.element.Equal(&b.element) && a.element.Equal(&c) && b.element.Equal(&c)
		},
		genA,
		genB,
	))

	properties.Property("Exp: operation result must match big.Int result", prop.ForAll(
		func(a, b testPaire_nocarry_0564) bool {
			{
				var c e_nocarry_0564

				c.Exp(a.element, &b.bigint)

				var d, e big.Int
				d.Exp(&a.bigint, &b.bigint, Modulus())

				if c.FromMont().ToBigInt(&e).Cmp(&d) != 0 {
					return false
				}
			}

			// fixed elements
			// a is random
			// r takes special values
			testValues := make([]e_nocarry_0564, len(staticTestValues))
			copy(testValues, staticTestValues)

			for _, r := range testValues {
				var d, e, rb big.Int
				r.ToBigIntRegular(&rb)

				var c e_nocarry_0564
				c.Exp(a.element, &rb)
				d.Exp(&a.bigint, &rb, Modulus())

				if c.FromMont().ToBigInt(&e).Cmp(&d) != 0 {
					return false
				}
			}
			return true
		},
		genA,
		genB,
	))

	properties.Property("Exp: operation result must be smaller than modulus", prop.ForAll(
		func(a, b testPaire_nocarry_0564) bool {
			var c e_nocarry_0564

			c.Exp(a.element, &b.bigint)

			return !c.biggerOrEqualModulus()
		},
		genA,
		genB,
	))

	specialValueTest := func() {
		// test special values against special values
		testValues := make([]e_nocarry_0564, len(staticTestValues))
		copy(testValues, staticTestValues)

		for _, a := range testValues {
			var aBig big.Int
			a.ToBigIntRegular(&aBig)
			for _, b := range testValues {

				var bBig, d, e big.Int
				b.ToBigIntRegular(&bBig)

				var c e_nocarry_0564
				c.Exp(a, &bBig)
				d.Exp(&aBig, &bBig, Modulus())

				if c.FromMont().ToBigInt(&e).Cmp(&d) != 0 {
					t.Fatal("Exp failed special test values")
				}
			}
		}
	}

	properties.TestingRun(t, gopter.ConsoleReporter(false))
	specialValueTest()
	// if we have ADX instruction enabled, test both path in assembly
	if supportAdx {
		t.Log("disabling ADX")
		supportAdx = false
		properties.TestingRun(t, gopter.ConsoleReporter(false))
		specialValueTest()
		supportAdx = true
	}
}

func TestE_nocarry_0564Square(t *testing.T) {
	parameters := gopter.DefaultTestParameters()
	if testing.Short() {
		parameters.MinSuccessfulTests = nbFuzzShort
	} else {
		parameters.MinSuccessfulTests = nbFuzz
	}

	properties := gopter.NewProperties(parameters)

	genA := gen()

	properties.Property("Square: having the receiver as operand should output the same result", prop.ForAll(
		func(a testPaire_nocarry_0564) bool {

			var b e_nocarry_0564

			b.Square(&a.element)
			a.element.Square(&a.element)
			return a.element.Equal(&b)
		},
		genA,
	))

	properties.Property("Square: operation result must match big.Int result", prop.ForAll(
		func(a testPaire_nocarry_0564) bool {
			var c e_nocarry_0564
			c.Square(&a.element)

			var d, e big.Int
			d.Mul(&a.bigint, &a.bigint).Mod(&d, Modulus())

			return c.FromMont().ToBigInt(&e).Cmp(&d) == 0
		},
		genA,
	))

	properties.Property("Square: operation result must be smaller than modulus", prop.ForAll(
		func(a testPaire_nocarry_0564) bool {
			var c e_nocarry_0564
			c.Square(&a.element)
			return !c.biggerOrEqualModulus()
		},
		genA,
	))

	specialValueTest := func() {
		// test special values
		testValues := make([]e_nocarry_0564, len(staticTestValues))
		copy(testValues, staticTestValues)

		for _, a := range testValues {
			var aBig big.Int
			a.ToBigIntRegular(&aBig)
			var c e_nocarry_0564
			c.Square(&a)

			var d, e big.Int
			d.Mul(&aBig, &aBig).Mod(&d, Modulus())

			if c.FromMont().ToBigInt(&e).Cmp(&d) != 0 {
				t.Fatal("Square failed special test values")
			}
		}
	}

	properties.TestingRun(t, gopter.ConsoleReporter(false))
	specialValueTest()
	// if we have ADX instruction enabled, test both path in assembly
	if supportAdx {
		supportAdx = false
		t.Log("disabling ADX")
		properties.TestingRun(t, gopter.ConsoleReporter(false))
		specialValueTest()
		supportAdx = true
	}
}

func TestE_nocarry_0564Inverse(t *testing.T) {
	parameters := gopter.DefaultTestParameters()
	if testing.Short() {
		parameters.MinSuccessfulTests = nbFuzzShort
	} else {
		parameters.MinSuccessfulTests = nbFuzz
	}

	properties := gopter.NewProperties(parameters)

	genA := gen()

	properties.Property("Inverse: having the receiver as operand should output the same result", prop.ForAll(
		func(a testPaire_nocarry_0564) bool {

			var b e_nocarry_0564

			b.Inverse(&a.element)
			a.element.Inverse(&a.element)
			return a.element.Equal(&b)
		},
		genA,
	))

	properties.Property("Inverse: operation result must match big.Int result", prop.ForAll(
		func(a testPaire_nocarry_0564) bool {
			var c e_nocarry_0564
			c.Inverse(&a.element)

			var d, e big.Int
			d.ModInverse(&a.bigint, Modulus())

			return c.FromMont().ToBigInt(&e).Cmp(&d) == 0
		},
		genA,
	))

	properties.Property("Inverse: operation result must be smaller than modulus", prop.ForAll(
		func(a testPaire_nocarry_0564) bool {
			var c e_nocarry_0564
			c.Inverse(&a.element)
			return !c.biggerOrEqualModulus()
		},
		genA,
	))

	specialValueTest := func() {
		// test special values
		testValues := make([]e_nocarry_0564, len(staticTestValues))
		copy(testValues, staticTestValues)

		for _, a := range testValues {
			var aBig big.Int
			a.ToBigIntRegular(&aBig)
			var c e_nocarry_0564
			c.Inverse(&a)

			var d, e big.Int
			d.ModInverse(&aBig, Modulus())

			if c.FromMont().ToBigInt(&e).Cmp(&d) != 0 {
				t.Fatal("Inverse failed special test values")
			}
		}
	}

	properties.TestingRun(t, gopter.ConsoleReporter(false))
	specialValueTest()
	// if we have ADX instruction enabled, test both path in assembly
	if supportAdx {
		supportAdx = false
		t.Log("disabling ADX")
		properties.TestingRun(t, gopter.ConsoleReporter(false))
		specialValueTest()
		supportAdx = true
	}
}

func TestE_nocarry_0564Sqrt(t *testing.T) {
	parameters := gopter.DefaultTestParameters()
	if testing.Short() {
		parameters.MinSuccessfulTests = nbFuzzShort
	} else {
		parameters.MinSuccessfulTests = nbFuzz
	}

	properties := gopter.NewProperties(parameters)

	genA := gen()

	properties.Property("Sqrt: having the receiver as operand should output the same result", prop.ForAll(
		func(a testPaire_nocarry_0564) bool {

			b := a.element

			b.Sqrt(&a.element)
			a.element.Sqrt(&a.element)
			return a.element.Equal(&b)
		},
		genA,
	))

	properties.Property("Sqrt: operation result must match big.Int result", prop.ForAll(
		func(a testPaire_nocarry_0564) bool {
			var c e_nocarry_0564
			c.Sqrt(&a.element)

			var d, e big.Int
			d.ModSqrt(&a.bigint, Modulus())

			return c.FromMont().ToBigInt(&e).Cmp(&d) == 0
		},
		genA,
	))

	properties.Property("Sqrt: operation result must be smaller than modulus", prop.ForAll(
		func(a testPaire_nocarry_0564) bool {
			var c e_nocarry_0564
			c.Sqrt(&a.element)
			return !c.biggerOrEqualModulus()
		},
		genA,
	))

	specialValueTest := func() {
		// test special values
		testValues := make([]e_nocarry_0564, len(staticTestValues))
		copy(testValues, staticTestValues)

		for _, a := range testValues {
			var aBig big.Int
			a.ToBigIntRegular(&aBig)
			var c e_nocarry_0564
			c.Sqrt(&a)

			var d, e big.Int
			d.ModSqrt(&aBig, Modulus())

			if c.FromMont().ToBigInt(&e).Cmp(&d) != 0 {
				t.Fatal("Sqrt failed special test values")
			}
		}
	}

	properties.TestingRun(t, gopter.ConsoleReporter(false))
	specialValueTest()
	// if we have ADX instruction enabled, test both path in assembly
	if supportAdx {
		supportAdx = false
		t.Log("disabling ADX")
		properties.TestingRun(t, gopter.ConsoleReporter(false))
		specialValueTest()
		supportAdx = true
	}
}

func TestE_nocarry_0564Double(t *testing.T) {
	parameters := gopter.DefaultTestParameters()
	if testing.Short() {
		parameters.MinSuccessfulTests = nbFuzzShort
	} else {
		parameters.MinSuccessfulTests = nbFuzz
	}

	properties := gopter.NewProperties(parameters)

	genA := gen()

	properties.Property("Double: having the receiver as operand should output the same result", prop.ForAll(
		func(a testPaire_nocarry_0564) bool {

			var b e_nocarry_0564

			b.Double(&a.element)
			a.element.Double(&a.element)
			return a.element.Equal(&b)
		},
		genA,
	))

	properties.Property("Double: operation result must match big.Int result", prop.ForAll(
		func(a testPaire_nocarry_0564) bool {
			var c e_nocarry_0564
			c.Double(&a.element)

			var d, e big.Int
			d.Lsh(&a.bigint, 1).Mod(&d, Modulus())

			return c.FromMont().ToBigInt(&e).Cmp(&d) == 0
		},
		genA,
	))

	properties.Property("Double: operation result must be smaller than modulus", prop.ForAll(
		func(a testPaire_nocarry_0564) bool {
			var c e_nocarry_0564
			c.Double(&a.element)
			return !c.biggerOrEqualModulus()
		},
		genA,
	))

	properties.Property("Double: assembly implementation must be consistent with generic one", prop.ForAll(
		func(a testPaire_nocarry_0564) bool {
			var c, d e_nocarry_0564
			c.Double(&a.element)
			_doubleGeneric(&d, &a.element)
			return c.Equal(&d)
		},
		genA,
	))

	specialValueTest := func() {
		// test special values
		testValues := make([]e_nocarry_0564, len(staticTestValues))
		copy(testValues, staticTestValues)

		for _, a := range testValues {
			var aBig big.Int
			a.ToBigIntRegular(&aBig)
			var c e_nocarry_0564
			c.Double(&a)

			var d, e big.Int
			d.Lsh(&aBig, 1).Mod(&d, Modulus())

			// checking asm against generic impl
			var cGeneric e_nocarry_0564
			_doubleGeneric(&cGeneric, &a)
			if !cGeneric.Equal(&c) {
				t.Fatal("Double failed special test values: asm and generic impl don't match")
			}

			if c.FromMont().ToBigInt(&e).Cmp(&d) != 0 {
				t.Fatal("Double failed special test values")
			}
		}
	}

	properties.TestingRun(t, gopter.ConsoleReporter(false))
	specialValueTest()
	// if we have ADX instruction enabled, test both path in assembly
	if supportAdx {
		supportAdx = false
		t.Log("disabling ADX")
		properties.TestingRun(t, gopter.ConsoleReporter(false))
		specialValueTest()
		supportAdx = true
	}
}

func TestE_nocarry_0564Neg(t *testing.T) {
	parameters := gopter.DefaultTestParameters()
	if testing.Short() {
		parameters.MinSuccessfulTests = nbFuzzShort
	} else {
		parameters.MinSuccessfulTests = nbFuzz
	}

	properties := gopter.NewProperties(parameters)

	genA := gen()

	properties.Property("Neg: having the receiver as operand should output the same result", prop.ForAll(
		func(a testPaire_nocarry_0564) bool {

			var b e_nocarry_0564

			b.Neg(&a.element)
			a.element.Neg(&a.element)
			return a.element.Equal(&b)
		},
		genA,
	))

	properties.Property("Neg: operation result must match big.Int result", prop.ForAll(
		func(a testPaire_nocarry_0564) bool {
			var c e_nocarry_0564
			c.Neg(&a.element)

			var d, e big.Int
			d.Neg(&a.bigint).Mod(&d, Modulus())

			return c.FromMont().ToBigInt(&e).Cmp(&d) == 0
		},
		genA,
	))

	properties.Property("Neg: operation result must be smaller than modulus", prop.ForAll(
		func(a testPaire_nocarry_0564) bool {
			var c e_nocarry_0564
			c.Neg(&a.element)
			return !c.biggerOrEqualModulus()
		},
		genA,
	))

	properties.Property("Neg: assembly implementation must be consistent with generic one", prop.ForAll(
		func(a testPaire_nocarry_0564) bool {
			var c, d e_nocarry_0564
			c.Neg(&a.element)
			_negGeneric(&d, &a.element)
			return c.Equal(&d)
		},
		genA,
	))

	specialValueTest := func() {
		// test special values
		testValues := make([]e_nocarry_0564, len(staticTestValues))
		copy(testValues, staticTestValues)

		for _, a := range testValues {
			var aBig big.Int
			a.ToBigIntRegular(&aBig)
			var c e_nocarry_0564
			c.Neg(&a)

			var d, e big.Int
			d.Neg(&aBig).Mod(&d, Modulus())

			// checking asm against generic impl
			var cGeneric e_nocarry_0564
			_negGeneric(&cGeneric, &a)
			if !cGeneric.Equal(&c) {
				t.Fatal("Neg failed special test values: asm and generic impl don't match")
			}

			if c.FromMont().ToBigInt(&e).Cmp(&d) != 0 {
				t.Fatal("Neg failed special test values")
			}
		}
	}

	properties.TestingRun(t, gopter.ConsoleReporter(false))
	specialValueTest()
	// if we have ADX instruction enabled, test both path in assembly
	if supportAdx {
		supportAdx = false
		t.Log("disabling ADX")
		properties.TestingRun(t, gopter.ConsoleReporter(false))
		specialValueTest()
		supportAdx = true
	}
}

func TestE_nocarry_0564Halve(t *testing.T) {

	parameters := gopter.DefaultTestParameters()
	if testing.Short() {
		parameters.MinSuccessfulTests = nbFuzzShort
	} else {
		parameters.MinSuccessfulTests = nbFuzz
	}

	properties := gopter.NewProperties(parameters)

	genA := gen()
	var twoInv e_nocarry_0564
	twoInv.SetUint64(2)
	twoInv.Inverse(&twoInv)

	properties.Property("z.Halve must match z / 2", prop.ForAll(
		func(a testPaire_nocarry_0564) bool {
			c := a.element
			d := a.element
			c.Halve()
			d.Mul(&d, &twoInv)
			return c.Equal(&d)
		},
		genA,
	))

	properties.TestingRun(t, gopter.ConsoleReporter(false))
}

func TestE_nocarry_0564FromMont(t *testing.T) {

	parameters := gopter.DefaultTestParameters()
	if testing.Short() {
		parameters.MinSuccessfulTests = nbFuzzShort
	} else {
		parameters.MinSuccessfulTests = nbFuzz
	}

	properties := gopter.NewProperties(parameters)

	genA := gen()

	properties.Property("Assembly implementation must be consistent with generic one", prop.ForAll(
		func(a testPaire_nocarry_0564) bool {
			c := a.element
			d := a.element
			c.FromMont()
			_fromMontGeneric(&d)
			return c.Equal(&d)
		},
		genA,
	))

	properties.Property("x.FromMont().ToMont() == x", prop.ForAll(
		func(a testPaire_nocarry_0564) bool {
			c := a.element
			c.FromMont().ToMont()
			return c.Equal(&a.element)
		},
		genA,
	))

	properties.TestingRun(t, gopter.ConsoleReporter(false))
}

type testPaire_nocarry_0564 struct {
	element e_nocarry_0564
	bigint  big.Int
}

func (z *e_nocarry_0564) biggerOrEqualModulus() bool {
	if z[8] > qe_nocarry_0564[8] {
		return true
	}
	if z[8] < qe_nocarry_0564[8] {
		return false
	}

	if z[7] > qe_nocarry_0564[7] {
		return true
	}
	if z[7] < qe_nocarry_0564[7] {
		return false
	}

	if z[6] > qe_nocarry_0564[6] {
		return true
	}
	if z[6] < qe_nocarry_0564[6] {
		return false
	}

	if z[5] > qe_nocarry_0564[5] {
		return true
	}
	if z[5] < qe_nocarry_0564[5] {
		return false
	}

	if z[4] > qe_nocarry_0564[4] {
		return true
	}
	if z[4] < qe_nocarry_0564[4] {
		return false
	}

	if z[3] > qe_nocarry_0564[3] {
		return true
	}
	if z[3] < qe_nocarry_0564[3] {
		return false
	}

	if z[2] > qe_nocarry_0564[2] {
		return true
	}
	if z[2] < qe_nocarry_0564[2] {
		return false
	}

	if z[1] > qe_nocarry_0564[1] {
		return true
	}
	if z[1] < qe_nocarry_0564[1] {
		return false
	}

	return z[0] >= qe_nocarry_0564[0]
}

func gen() gopter.Gen {
	return func(genParams *gopter.GenParameters) *gopter.GenResult {
		var g testPaire_nocarry_0564

		g.element = e_nocarry_0564{
			genParams.NextUint64(),
			genParams.NextUint64(),
			genParams.NextUint64(),
			genParams.NextUint64(),
			genParams.NextUint64(),
			genParams.NextUint64(),
			genParams.NextUint64(),
			genParams.NextUint64(),
			genParams.NextUint64(),
		}
		if qe_nocarry_0564[8] != ^uint64(0) {
			g.element[8] %= (qe_nocarry_0564[8] + 1)
		}

		for g.element.biggerOrEqualModulus() {
			g.element = e_nocarry_0564{
				genParams.NextUint64(),
				genParams.NextUint64(),
				genParams.NextUint64(),
				genParams.NextUint64(),
				genParams.NextUint64(),
				genParams.NextUint64(),
				genParams.NextUint64(),
				genParams.NextUint64(),
				genParams.NextUint64(),
			}
			if qe_nocarry_0564[8] != ^uint64(0) {
				g.element[8] %= (qe_nocarry_0564[8] + 1)
			}
		}

		g.element.ToBigIntRegular(&g.bigint)
		genResult := gopter.NewGenResult(g, gopter.NoShrinker)
		return genResult
	}
}

func genFull() gopter.Gen {
	return func(genParams *gopter.GenParameters) *gopter.GenResult {

		genRandomFq := func() e_nocarry_0564 {
			var g e_nocarry_0564

			g = e_nocarry_0564{
				genParams.NextUint64(),
				genParams.NextUint64(),
				genParams.NextUint64(),
				genParams.NextUint64(),
				genParams.NextUint64(),
				genParams.NextUint64(),
				genParams.NextUint64(),
				genParams.NextUint64(),
				genParams.NextUint64(),
			}

			if qe_nocarry_0564[8] != ^uint64(0) {
				g[8] %= (qe_nocarry_0564[8] + 1)
			}

			for g.biggerOrEqualModulus() {
				g = e_nocarry_0564{
					genParams.NextUint64(),
					genParams.NextUint64(),
					genParams.NextUint64(),
					genParams.NextUint64(),
					genParams.NextUint64(),
					genParams.NextUint64(),
					genParams.NextUint64(),
					genParams.NextUint64(),
					genParams.NextUint64(),
				}
				if qe_nocarry_0564[8] != ^uint64(0) {
					g[8] %= (qe_nocarry_0564[8] + 1)
				}
			}

			return g
		}
		a := genRandomFq()

		var carry uint64
		a[0], carry = bits.Add64(a[0], qe_nocarry_0564[0], carry)
		a[1], carry = bits.Add64(a[1], qe_nocarry_0564[1], carry)
		a[2], carry = bits.Add64(a[2], qe_nocarry_0564[2], carry)
		a[3], carry = bits.Add64(a[3], qe_nocarry_0564[3], carry)
		a[4], carry = bits.Add64(a[4], qe_nocarry_0564[4], carry)
		a[5], carry = bits.Add64(a[5], qe_nocarry_0564[5], carry)
		a[6], carry = bits.Add64(a[6], qe_nocarry_0564[6], carry)
		a[7], carry = bits.Add64(a[7], qe_nocarry_0564[7], carry)
		a[8], _ = bits.Add64(a[8], qe_nocarry_0564[8], carry)

		genResult := gopter.NewGenResult(a, gopter.NoShrinker)
		return genResult
	}
}

//this is a hack so that there isn't an import error in case mrand is not used
//TODO: Do it properly
func useMRand() {
	_ = mrand.Uint64()
}

func Benchmarke_nocarry_0564InverseNew(b *testing.B) {
	var x e_nocarry_0564
	x.SetRandom()

	b.Run("inverseNew", func(b *testing.B) {
		b.ResetTimer()
		for i := 0; i < b.N; i++ {
			benchRese_nocarry_0564.Inverse(&x)
		}
	})
}

func TestP20InversionApproximation(t *testing.T) {
	var x e_nocarry_0564
	for i := 0; i < 1000; i++ {
		x.SetRandom()

		//Normally small elements are unlikely. Here we give them a higher chance
		xZeros := mrand.Int() % Limbs
		for j := 1; j < xZeros; j++ {
			x[Limbs-j] = 0
		}

		a := approximate(&x, x.BitLen())
		aRef := approximateRef(&x)

		if a != aRef {
			t.Fatal("Approximation mismatch")
		}
	}
}

func TestP20InversionCorrectionFactorFormula(t *testing.T) {
	const iterationN = 2 * ((2*Bits-2)/(2*k) + 1) // 2  ⌈ (2 * field size - 1) / 2k ⌉
	const kLimbs = k * Limbs
	const power = kLimbs*6 + iterationN*(kLimbs-k+1)
	factorInt := big.NewInt(1)
	factorInt.Lsh(factorInt, power)
	factorInt.Mod(factorInt, Modulus())

	var refFactorInt big.Int
	inversionCorrectionFactor.ToBigInt(&refFactorInt)

	if refFactorInt.Cmp(factorInt) != 0 {
		t.Fatal("mismatch")
	}
}

func TestLinearComb(t *testing.T) {
	var x e_nocarry_0564
	var y e_nocarry_0564

	for i := 0; i < 1000; i++ {
		x.SetRandom()
		y.SetRandom()
		testLinearComb(t, &x, mrand.Int63(), &y, mrand.Int63())
	}
}

//Probably unnecessary post-dev. In case the output of inv is wrong, this checks whether it's only off by a constant factor.
func TestP20InversionCorrectionFactor(t *testing.T) {

	//(1/x)/inv(x) = (1/1)/inv(1) ⇔ inv(1) = x inv(x)

	var one e_nocarry_0564
	var oneInv e_nocarry_0564
	one.SetOne()
	oneInv.Inverse(&one)

	for i := 0; i < 100; i++ {
		var x e_nocarry_0564
		var xInv e_nocarry_0564
		x.SetRandom()
		xInv.Inverse(&x)

		x.Mul(&x, &xInv)
		if !x.Equal(&oneInv) {
			t.Fatal("Correction factor is inconsistent")
		}
	}

	if !oneInv.Equal(&one) {
		var i big.Int
		oneInv.ToBigIntRegular(&i) //no montgomery
		i.ModInverse(&i, Modulus())
		var fac e_nocarry_0564
		fac.setBigInt(&i) //back to montgomery

		var facTimesFac e_nocarry_0564
		facTimesFac.Mul(&inversionCorrectionFactor, &fac)

		t.Fatal("Correction factor is consistently off by", fac, "Should be", facTimesFac)
	}
}

func TestBigNumNeg(t *testing.T) {
	var a e_nocarry_0564
	aHi := a.neg(&a, 0)
	if !a.IsZero() || aHi != 0 {
		t.Fatal("-0 != 0")
	}
}

func TestBigNumWMul(t *testing.T) {
	var x e_nocarry_0564

	for i := 0; i < 1000; i++ {
		x.SetRandom()
		w := mrand.Int63()
		testBigNumWMul(t, &x, w)
	}
}

func TestBigNumWMulBr(t *testing.T) {
	var x e_nocarry_0564

	for i := 0; i < 1000; i++ {
		x.SetRandom()
		w := mrand.Int63()
		testBigNumWMulBr(t, &x, w)
	}
}

func TestVeryBigIntConversion(t *testing.T) {
	xHi := mrand.Uint64()
	var x e_nocarry_0564
	x.SetRandom()
	var xInt big.Int
	x.toVeryBigIntSigned(&xInt, xHi)
	x.assertMatchVeryBigInt(t, xHi, &xInt)
}

func TestMontReducePos(t *testing.T) {
	var x e_nocarry_0564

	for i := 0; i < 1000; i++ {
		x.SetRandom()
		testMontReduceSigned(t, &x, mrand.Uint64() & ^signBitSelector)
	}
}

func TestMonReduceNeg(t *testing.T) {
	var x e_nocarry_0564

	for i := 0; i < 1000; i++ {
		x.SetRandom()
		testMontReduceSigned(t, &x, mrand.Uint64()|signBitSelector)
	}
}

func TestMontNegMultipleOfR(t *testing.T) {
	var zero e_nocarry_0564

	for i := 0; i < 1000; i++ {
		testMontReduceSigned(t, &zero, mrand.Uint64()|signBitSelector)
	}
}

func testLinearComb(t *testing.T, x *e_nocarry_0564, xC int64, y *e_nocarry_0564, yC int64) {

	var p1 big.Int
	x.ToBigInt(&p1)
	p1.Mul(&p1, big.NewInt(xC))

	var p2 big.Int
	y.ToBigInt(&p2)
	p2.Mul(&p2, big.NewInt(yC))

	p1.Add(&p1, &p2)
	p1.Mod(&p1, Modulus())
	montReduce(&p1, &p1)

	var z e_nocarry_0564
	z.linearCombSosSigned(x, xC, y, yC)
	z.assertMatchVeryBigInt(t, 0, &p1)

}

func testBigNumWMulBr(t *testing.T, a *e_nocarry_0564, c int64) {
	var aHi uint64
	var aTimes e_nocarry_0564
	aHi = aTimes.mulWRegularBr(a, c)

	assertMulProduct(t, a, c, &aTimes, aHi)
}

func testBigNumWMul(t *testing.T, a *e_nocarry_0564, c int64) {
	var aHi uint64
	var aTimes e_nocarry_0564
	aHi = aTimes.mulWRegular(a, c)

	assertMulProduct(t, a, c, &aTimes, aHi)
}

func testMontReduceSigned(t *testing.T, x *e_nocarry_0564, xHi uint64) {
	var res e_nocarry_0564
	var xInt big.Int
	var resInt big.Int
	x.toVeryBigIntSigned(&xInt, xHi)
	res.montReduceSigned(x, xHi)
	montReduce(&resInt, &xInt)
	res.assertMatchVeryBigInt(t, 0, &resInt)
}

var rInv big.Int

func montReduce(res *big.Int, x *big.Int) {
	if rInv.BitLen() == 0 { //initialization
		rInv.SetUint64(1)
		rInv.Lsh(&rInv, Limbs*bits.UintSize)
		rInv.ModInverse(&rInv, Modulus())
	}
	res.Mul(x, &rInv)
	res.Mod(res, Modulus())
}

func (z *e_nocarry_0564) toVeryBigIntUnsigned(i *big.Int, xHi uint64) {
	z.ToBigInt(i)
	var upperWord big.Int
	upperWord.SetUint64(xHi)
	upperWord.Lsh(&upperWord, Limbs*bits.UintSize)
	i.Add(&upperWord, i)
}

func (z *e_nocarry_0564) toVeryBigIntSigned(i *big.Int, xHi uint64) {
	z.toVeryBigIntUnsigned(i, xHi)
	if signBitSelector&xHi != 0 {
		twosCompModulus := big.NewInt(1)
		twosCompModulus.Lsh(twosCompModulus, (Limbs+1)*bits.UintSize)
		i.Sub(i, twosCompModulus)
	}
}

func assertMulProduct(t *testing.T, x *e_nocarry_0564, c int64, result *e_nocarry_0564, resultHi uint64) big.Int {
	var xInt big.Int
	x.ToBigInt(&xInt)

	xInt.Mul(&xInt, big.NewInt(c))

	result.assertMatchVeryBigInt(t, resultHi, &xInt)
	return xInt
}

func assertMatch(t *testing.T, w []big.Word, a uint64, index int) {
	var wI big.Word

	if index < len(w) {
		wI = w[index]
	}

	if uint64(wI) != a {
		t.Fatal("Bignum mismatch: disagreement on word", index)
	}
}

func (z *e_nocarry_0564) assertMatchVeryBigInt(t *testing.T, aHi uint64, aInt *big.Int) {

	if bits.UintSize != 64 {
		panic("Word size 64 expected")
	}

	var modulus big.Int
	var aIntMod big.Int
	modulus.SetInt64(1)
	modulus.Lsh(&modulus, (Limbs+1)*64)
	aIntMod.Mod(aInt, &modulus)

	words := aIntMod.Bits()

	for i := 0; i < Limbs; i++ {
		assertMatch(t, words, z[i], i)
	}

	assertMatch(t, words, aHi, Limbs)
}

func approximateRef(x *e_nocarry_0564) uint64 {

	var asInt big.Int
	x.ToBigInt(&asInt)
	n := x.BitLen()

	if n <= 64 {
		return asInt.Uint64()
	}

	modulus := big.NewInt(1 << 31)
	var lo big.Int
	lo.Mod(&asInt, modulus)

	modulus.Lsh(modulus, uint(n-64))
	var hi big.Int
	hi.Div(&asInt, modulus)
	hi.Lsh(&hi, 31)

	hi.Add(&hi, &lo)
	return hi.Uint64()
}
